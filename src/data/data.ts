export const paintCanSizes = {
  exLarge: 18,
  large: 3.6,
  medium: 2.5,
  small: 0.5,
};

// (height * width) > 1 && (height * width) <= 50
export const wallOne = {
  height: Number,
  width: Number,
  doors: Number,
  windows: Number,
};

export const wallTwo = {
  height: Number,
  width: Number,
  doors: Number,
  windows: Number,
};

export const wallThree = {
  height: Number,
  width: Number,
  doors: Number,
  windows: Number,
};

export const wallFour = {
  height: Number,
  width: Number,
  doors: Number,
  windows: Number,
};

export const wallSize = {
  height: 1.9,
  width: 0.8,
};

export const windowSize = {
  height: 2,
  width: 1.2,
};
